{% extends 'profile.html.twig' %}

{% block main %}

    <h1 class="text-secondary">Berichten</h1> 

    {% include 'post/template.html.twig' with {'item': post } %}

    <ul class="list-inline">
      <li class="list-inline-item">
        <a href="{{ path('profile_posts') }}"><i class="bi bi-chevron-double-left"></i> terug naar het overzicht</a>
      </li>
    </ul>

{% endblock %}
