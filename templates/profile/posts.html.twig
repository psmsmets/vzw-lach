{% extends 'profile.html.twig' %}

{% block main %}
    <h1 class="text-secondary">Berichten</h1> 

    {% for post in posts %}
      <h4 class="mt-4">{{post.title}}</h4>
      {{post.body|raw}}
    {% endfor %}

{% endblock %}
